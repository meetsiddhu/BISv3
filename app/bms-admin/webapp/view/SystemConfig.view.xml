<mvc:View
  controllerName="BridgeManagement.bmsadmin.controller.SystemConfig"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m"
  displayBlock="true">

  <Page id="systemConfigPage" showHeader="true">

    <customHeader>
      <Bar>
        <contentLeft>
          <Title text="{i18n>admin.systemConfig.title}" level="H3" />
        </contentLeft>
        <contentRight>
          <Button id="saveBtn" text="{i18n>sysConfig.save}" type="Emphasized" enabled="false"
                  press=".onSaveChanges" icon="sap-icon://save" />
          <Button text="{i18n>sysConfig.reset}" type="Transparent"
                  press=".onResetDefaults" icon="sap-icon://reset" class="sapUiSmallMarginBegin" />
          <Button icon="sap-icon://information" tooltip="{i18n>gis.help}" press=".onShowHelp" type="Transparent" class="sapUiSmallMarginBegin" />
        </contentRight>
      </Bar>
    </customHeader>

    <content>
      <VBox class="sapUiSmallMargin">
        <MessageStrip
          text="{i18n>sysConfig.info}"
          type="Information" showCloseButton="false" class="sapUiSmallMarginBottom" />

        <IconTabBar id="categoryTabs" select=".onCategoryChange" class="sapUiSmallMarginBottom" />

        <Table id="configTable" mode="None" items="{/filtered}" noDataText="{i18n>sysConfig.noData}">
          <columns>
            <Column width="22%"><Text text="{i18n>sysConfig.col.setting}" /></Column>
            <Column width="25%"><Text text="{i18n>sysConfig.col.currentValue}" /></Column>
            <Column width="18%"><Text text="{i18n>sysConfig.col.default}" /></Column>
            <Column width="10%"><Text text="{i18n>sysConfig.col.dataType}" /></Column>
            <Column width="25%"><Text text="{i18n>sysConfig.col.description}" /></Column>
          </columns>
          <items>
            <ColumnListItem>
              <cells>
                <VBox>
                  <Text text="{label}" />
                  <Text text="{configKey}" class="sapUiTinyMarginTop" />
                </VBox>
                <HBox alignItems="Center">
                  <CheckBox
                    visible="{= ${dataType} === 'boolean'}"
                    selected="{path: 'value', formatter: '.formatBoolValue'}"
                    select=".onCheckBoxChange"
                    enabled="{= !${isReadOnly}}" />
                  <Input
                    visible="{= ${dataType} !== 'boolean'}"
                    value="{value}"
                    type="{= ${dataType} === 'integer' || ${dataType} === 'decimal' ? 'Number' : 'Text'}"
                    editable="{= !${isReadOnly}}"
                    change=".onValueChange"
                    width="100%" />
                </HBox>
                <Text text="{defaultValue}" />
                <ObjectStatus text="{dataType}" />
                <Text text="{description}" wrapping="true" />
              </cells>
            </ColumnListItem>
          </items>
        </Table>
      </VBox>
    </content>
  </Page>

</mvc:View>
